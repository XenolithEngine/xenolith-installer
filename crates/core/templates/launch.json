{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(lldb) Run",
      "type": "lldb-dap",
      "request": "launch",
      "program": "${workspaceFolder}/{{BINARY_PATH}}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
